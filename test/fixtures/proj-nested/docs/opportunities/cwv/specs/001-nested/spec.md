---
status: DONE
---

# Spec 001: Nested thing

Fixture spec used to exercise a non-`docs` artifact root (spec 007-01, real
Pattern B shape) — this sub-project's jig artifacts live under
`docs/opportunities/cwv/` while the umbrella repo's own (sibling) `docs/`
content lives at the parent level and must never bleed into this card.
