---
status: DONE
---

# Spec 001: Track A widget

Fixture spec for track `a` of the synthetic multi-entry umbrella project
(spec 007-02, real Pattern C shape: `tracks/<name>/{specs,decisions}` with
no root `docs/specs`).
