# ADR 1: Track C decision

Fixture ADR for track `c` of the synthetic multi-entry umbrella project.
Track `c` is deliberately decisions-only (no `specs/` dir) to exercise the
jig-managed-but-no-specs `unknown` execution path (spec 007-02 AC4) — never a
false `0/0`.
