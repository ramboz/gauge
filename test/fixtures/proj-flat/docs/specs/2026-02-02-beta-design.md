# Spec 7: Beta design

Status: APPROVED (brainstorm)

## Problem

A second flat design doc, whose heading carries a `Spec N:` prefix the flat
reader strips exactly as the nested reader does. No frontmatter status.
