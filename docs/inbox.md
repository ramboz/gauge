# Inbox

> Status: Draft (wizard-generated)
>
> Thin capture layer for unresolved ideas, observations, and items that surfaced during
> sessions but aren't ready for a spec. Triage during reconciliation or session end:
> (a) promote to a spec, (b) promote to an ADR, (c) drop.
>
> This is NOT a task list. Items here are parked thoughts, not committed work.

<!-- Add items below. Format: - [date] description -->
- [2026-08-03] Gauge validation reference projects — standing corpus the user re-runs collection against when validating adapter / onboarding / multi-project work (registered in a local gauge.config.json during the 2026-08-03 POC exercise). PATTERN A (flat jig repo: docs/{specs,decisions}, 1 repo=1 card, works today): jig=/Users/ramboz/Projects/misc/jig (adapters:[jig], 100 specs); gauge=/Users/ramboz/Projects/misc/gauge ([jig], self-observation, 6 specs); servo=/Users/ramboz/Projects/misc/servo ([jig], no scaffold.json but jig-style docs/, 22 specs); shaper=/Users/ramboz/Projects/misc/shaper ([jig], 7 specs all DONE but ~3wk stale = freshness-blindness example). PATTERN B (nested sub-projects in a large repo): mystique=/Users/ramboz/Projects/spacecat/mystique (generic [] today; real jig artifacts live under docs/opportunities/cwv/{specs,bugs,decisions}=12 specs/26 ADRs + docs/superpowers/specs; root docs/specs has only 2 stray dirs so forcing [jig] yields a misleading 0/2). PATTERN C (umbrella multi-track workspace): personalization-workspace=/Users/ramboz/Projects/aem/personalization-workspace (tracks/{rtb,offer-management,contextual-experimentation}/{specs,decisions}=11 specs/14 ADRs; NO root docs/specs so gauge sees it as generic and is blind to all tracks; self-describes via repos.yaml scope tags; each track spans multiple external code repos).
- [2026-08-06] Appetite-based confidence, not just calendar deadlines — real portfolio signal from the 010 dashboard run: the user has no target dates; at best they have shaper releases that are appetite-based (fixed-effort / cutline), not time-based. Today the forecast/attention model (ADR-0012/ADR-0013, deriveForecast) requires a concrete ISO `deadline` — anything else (including "unknown") collapses every card to `forecast: unknown (deadline-unknown)` → attention tier 3 "needs a deadline set". So a portfolio run entirely on appetite/release shaping surfaces zero differentiated confidence, which is the common case here. Possible directions (not yet shaped): (a) a shaper adapter that reads release appetite/cutline as an alternate confidence input; (b) a scope-burndown forecast that derives confidence from workstream step velocity vs. remaining scope even with deadline=unknown; (c) an attention tier that ranks appetite-shaped tracks by progress/staleness instead of demoting them all to "needs a deadline". Relates to spec 010 (entry-level goal/deadline now exists but stays inert without a date) and ADR-0011's deliberate "machine never infers a deadline" stance. **— RESOLVED 2026-08-13 by [ADR-0018: Date-independent forecast for appetite-shaped work](decisions/adr-0018-date-independent-forecast.md):** a tiered model — committed hard deadline (green/red), committed **soft appetite-window** (green/amber, curated at onboarding per ADR-0011, no runtime prose parsing), else neutral date-free pace (advancing/stalled, no colour/urgency) or `unknown`. Directions (a)/(b)/(c) were each rejected or reshaped: (a) runtime appetite-parsing violates ADR-0011; (b) date-free pace kept but neutral-only (git-backfill makes it computable on 31–71% of observations); (c) attention re-tiering rejected as urgency-laundering. Implementation is a follow-up slice sequenced with the git-backfill seed.
- [2026-08-13] Shared CLI-scaffold helper (ADR-0002 third-caller) — `parseArgs` plus the config-load / warn / filter / exit scaffold now recurs verbatim across `scripts/snapshot.mjs`, `scripts/onboard.mjs`, and `scripts/backfill.mjs` (added by slice 013-01). This crosses ADR-0002's extract-on-third-caller threshold; extract a shared `scripts/_cli.mjs` (or `src/cli-lib.mjs`) helper. Follow-up, not blocking 013.
- [2026-08-13] Target-descriptor refactor trigger (from 013-03 arch review) — Gauge now threads TWO curated absolute-date targets (`deadline`, `appetiteWindow`) through ~7 parallel `deadline`-shaped sites (schema ×2, profile validation ×2, config resolve + entry fallback, observation join, derive join) plus duplicated pace arithmetic in `deriveForecast` tier-1/tier-2 blocks. Correct + readable for two; a THIRD curated target should trigger collapsing to a target-descriptor list (name + `$def` + tier + reason-pair) rather than a third parallel thread. Non-blocking; a refactor trigger, not a defect.
